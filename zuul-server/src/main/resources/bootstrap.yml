spring:
  application:
    name: zuul-server
  cloud:
     config:
           uri: http://localhost:7001
---
spring:
  profiles: docker
  cloud:
     config:
        uri: http://config-server:7001           